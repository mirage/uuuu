(menhir
 ((modules (format_a_parser))))

(ocamllex
 ((modules (format_a_lexer))))

(executable
 ((name generate)
  (public_name uuuu.generate)
  (libraries (fmt bos rresult cmdliner ptmap angstrom))))
